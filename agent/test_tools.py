def test_mock_shopify_inventory():
    # Simulating the fake data your tool normally returns
    mock_stock_count = 15
    
    # 'assert' tells Python to verify this statement is True
    # If it is not True, the test fails and alerts you.
    assert mock_stock_count == 15